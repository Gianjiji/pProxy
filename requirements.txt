# Privacy Proxy per LLM - Dipendenze
# Installare con: pip install -r requirements.txt
#
# IL CORE NON HA DIPENDENZE ESTERNE OBBLIGATORIE.
# Lo strumento funziona con la sola libreria standard di Python (>=3.9):
# anonimizzazione regex, mappa in chiaro, ripristino e provider 'demo' non
# richiedono alcun pacchetto di terzi. Tutte le dipendenze qui sotto sono
# OPZIONALI: il codice degrada in modo graceful (try/except ImportError) e
# avvisa se una funzionalità richiede un pacchetto non installato.
#
# Decommentare solo i blocchi delle funzionalità che servono, oppure seguire
# le istruzioni puntuali nel README ("Dipendenze opzionali").

# ── Testing (per eseguire la suite inclusa) ───────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Parsing documenti (opzionale) ─────────────────────────
 pdfplumber>=0.10.0          # Estrazione testo da PDF
 pymupdf>=1.24.0             # Alternativa per PDF (fitz)
 pytesseract>=0.3.10         # OCR per PDF scansionati (richiede tesseract-ocr di sistema)
 pdf2image>=1.17.0           # Conversione pagine PDF in immagini per OCR
 python-docx>=1.1.0          # Lettura file DOCX
 pandas>=2.0.0               # Lettura CSV con inferenza colonne

# ── NER – Named Entity Recognition (opzionale) ────────────
 spacy>=3.7.0                # Modello NER (anche: python -m spacy download it_core_news_sm)
 gliner>=0.2.0               # GLiNER zero-shot NER multilingue (urchade/gliner_multi-v2.1)
 presidio-analyzer>=2.2.0    # Microsoft Presidio per rilevamento entità
 presidio-anonymizer>=2.2.0  # Presidio anonimizzatore

# ── Cifratura mappa (opzionale) ───────────────────────────
 cryptography>=42.0.0        # AES-256 per la mappa sicura (Fernet)

# ── Provider LLM (opzionale, uno a scelta) ────────────────
# openai>=1.30.0              # OpenAI (GPT-4, GPT-4o, ecc.)
# anthropic>=0.28.0           # Anthropic (Claude)
# google-generativeai>=0.7.0  # Google (Gemini)

# ── HTTP per Ollama (opzionale) ───────────────────────────
# requests>=2.31.0            # Chiamate HTTP a Ollama locale

# Dipendenze della web app (opzionali, non necessarie per CLI/libreria core).
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.25.0          # usato anche da fastapi.testclient nei test
python-multipart>=0.0.9  # upload di file (multipart/form-data)
